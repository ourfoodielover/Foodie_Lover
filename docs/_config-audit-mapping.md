# Configuration Audit — Mapping Table (internal working doc)

Legend: **A** = ENV + Admin + Default (ENV wins). **B** = Admin + Default only (no ENV). **C** = ENV-only (secret/infra).

| Setting | ENV var(s) | Admin/DB field | Current consumers | Current priority | Expected priority | Class |
|---|---|---|---|---|---|---|
| Restaurant display name | `RESTAURANT_NAME` (server), `NEXT_PUBLIC_RESTAURANT_NAME` (client, **currently unset/broken**) | `restaurant_settings.restaurant_name` (seeded, **never read**) | `lib/email-server.ts` (module-const, ~20 templates), `app/api/ai/chat/route.ts`, `app/api/rewards/spin-invite/route.ts` (x2), `app/spin/page.tsx` (client, broken var) | Scattered `process.env.RESTAURANT_NAME ?? 'Foodie Lover'` duplicated in 4 files; DB value dead; client var never populated | **A** — ENV > Admin DB > 'Foodie Lover' | **A** |
| Restaurant tenant ID | `NEXT_PUBLIC_RESTAURANT_ID` | — (none; this *is* the tenant selector) | ~20 files, both client (realtime channel identity) and server | ENV-only, correct already | ENV > Default `'rest_default'` (no Admin — changing tenant isn't an Admin setting) | **ENV+Default** (not A/B/C — infra) |
| App base URL | `NEXT_PUBLIC_APP_URL` | — | `lib/email-server.ts` (tracking/spin links), `lib/api.ts` (server-side fetch base), `app/api/rewards/spin-invite/route.ts` | ENV-only, correct already | ENV > Default `http://localhost:3000` (no Admin — must match actual deployed domain; NEXT_PUBLIC build-time) | **ENV+Default** |
| Kitchen routing mode | — | `restaurants.kitchen_mode` | `app/api/admin/restaurant-config`, waiter/kitchen/admin dashboards | Admin > Default `'ask'`, already dynamic (polled every 5s), correct already | Admin > Default (no ENV — explicitly an operational setting per spec) | **B** (already correct) |
| Spin & Win config (enabled, min_order_amount, eligible types, require email/phone) | — | `spin_config` table via `/api/admin/spin-config` | `app/api/rewards/eligibility`, `spin-invite`, `spin-verify`, `spin` routes, Admin UI | Admin > Default, correct already; `min_order_amount` used via unchecked `Number()` | Admin > Default (no ENV) | **B** (already correct; numeric parsing hardened) |
| Offers / coupon rules | — | `offers` table | `app/api/offers`, Admin UI | Admin > Default, correct | Admin > Default (no ENV) | **B** |
| Email sender address | `FROM_EMAIL` | `restaurant_settings.receipt_email_from` (seeded, **never read**) | `lib/email.ts` (deliberately ENV-only — see file's own doc comment: must be a Resend-verified domain, no fallback) | ENV-only by design | **Stays ENV-only** — Admin fallback would let an unverified/unauthorized address silently break all email delivery | **C**-like (infra, non-secret) |
| Resend API key | `RESEND_API_KEY` | — | `lib/email.ts`, `lib/email-server.ts`, `app/api/email/diagnostics` | ENV-only, secret | ENV-only | **C** |
| Gemini AI API key | `GEMINI_API_KEY` | — | `app/api/ai/chat/route.ts` | ENV-only, secret | ENV-only | **C** |
| Supabase URL / anon key | `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` | — | `lib/supabase.ts` (browser client) | ENV-only, public-by-design (RLS-protected) | ENV-only | **C** (infra, not secret, but not Admin-editable) |
| Supabase service-role key | `SUPABASE_SERVICE_ROLE_KEY` | — | `lib/supabase-server.ts` | ENV-only, secret | ENV-only, **must never reach client/Admin response** | **C** |
| Cron secret | `CRON_SECRET` | — | `app/api/email/process-queue` | ENV-only, secret | ENV-only | **C** |
| Print agent auth key | `PRINT_AGENT_KEY` | — | `app/api/print-jobs`, `app/api/print-jobs/[id]` | ENV-only, secret | ENV-only | **C** |
| Admin / Kitchen / Manager PIN | `ADMIN_PIN`, `KITCHEN_PIN`, `MANAGER_PIN` | `restaurant_settings.{admin_pin,kitchen_pin,manager_pin}` | `app/api/init` (one-time seed only), `app/api/auth/verify-pin` etc. (Admin DB is authoritative after seed) | ENV seeds once on first init; Admin DB authoritative forever after — **intentionally NOT "ENV always wins" for security/usability reasons**, see report §7 | Admin authoritative after first-run seed (unchanged) | **Special case** — documented deliberate exception |
| Owner PIN | — | `restaurant_settings.owner_pin` | seeded only, no route consumes it today | dead | Admin > Default (no ENV) | **B** (currently unused) |
| Security question/answer | — | `restaurant_settings.{security_question,security_answer,security_setup_at}` | `app/api/auth/recover-pin`, Admin UI | Admin-only, correct | Admin-only (no ENV, no default — sensitive) | **B** |
| Tax rate | — | `restaurant_settings.tax_rate` | seeded only, **no consumer** — not applied to any price calc today | dead | Admin > Default `5` (no ENV per spec's "don't over-add ENV to operational settings") — **not wired to pricing logic; out of scope for this fix, see report §16** | **B** (not yet functionally wired) |
| Currency / currency symbol | — | `restaurant_settings.{currency,currency_symbol}` | seeded only, **no consumer** — ₹ is hardcoded literally in 24+ JSX locations | dead | Admin > Default (`INR`/`₹`) — **not wired; out of scope, see report §16** | **B** (not yet functionally wired) |
| Delivery enabled | — | `restaurant_settings.delivery_enabled` **vs** `allow_delivery` (two different key names seeded by two different code paths — duplication bug) | seeded only, **no consumer** | dead, and duplicated key names | Admin > Default `true` — **normalize to one key; not wired to gate order types, out of scope, see report §16** | **B** (duplication fixed, not yet functionally wired) |
| Pickup enabled | — | `restaurant_settings.pickup_enabled` **vs** `allow_pickup` (same duplication) | seeded only, **no consumer** | dead, duplicated | Admin > Default `true` — normalize, not wired | **B** |
| Online ordering enabled | — | `restaurant_settings.online_ordering` | seeded only, **no consumer** | dead | Admin > Default `false` — not wired | **B** |
| Table ordering enabled | — | `restaurant_settings.allow_table_ordering` | seeded only, **no consumer** | dead | Admin > Default `true` — not wired | **B** |
| Email notifications toggle | — | `restaurant_settings.email_notifications` | seeded only, **no consumer** | dead | Admin > Default `false` — not wired | **B** |
| WhatsApp number | — | `restaurant_settings.whatsapp_number`, **also** `localStorage['fl_whatsapp_number']` (dead duplicate storage mechanism) | seeded only, **no consumer** | dead, duplicated storage | Admin > Default `''` — not wired | **B** |
| Service charge | — | `restaurant_settings.service_charge` | seeded only, **no consumer** | dead | Admin > Default `0` — not wired | **B** |
| Waiter call cooldown | — | `restaurant_settings.waiter_call_cooldown` | seeded only, **no consumer** | dead | Admin > Default `120` — not wired | **B** |
| Receipt email "from" override | — | `restaurant_settings.receipt_email_from` | seeded only, **no consumer** (see Email sender address above — deliberately not wired) | dead | Leave unwired — see Email sender address row | **B** (vestigial) |
| Print agent hardware config | `PRINTER_TYPE`, `PRINTER_NAME`, `PRINTER_DEV`, `PRINTER_IP`, `PRINTER_PORT`, `PRINTER_CHARS_PER_LINE`, `PRINTER_STATION_ID`, `MAX_ATTEMPTS`, `RESTAURANT_NAME` (own copy), `APP_BASE_URL`, `PRINT_AGENT_KEY`, `RESTAURANT_ID`, `POLL_INTERVAL_MS` | — | `print-agent/index.js` — a **separate standalone Node process** run on the restaurant's own PC, not part of the Next.js deployment | ENV-only, local machine `.env`, correct already | Out of scope for the server-side resolver (different process/deployment; can't reach `restaurant_settings` or the resolver at all) | **N/A — separate app** |
| `restaurants.name` | — | `restaurants.name` column | `app/api/init` (write-once at first seed), never read elsewhere | dead (distinct from `restaurant_settings.restaurant_name`, itself also dead) | Not part of resolver — superseded by the `restaurant_name` setting above; leave as an internal DB seed field | **N/A — internal, not surfaced** |
